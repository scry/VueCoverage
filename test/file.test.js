import { reversal } from '../src/file'
import { assert } from 'chai'

describe('file', function () {
  it ('should do stuff', function () {
    throw new Error('')
    assert(reversal('some string')).equal('gnirts emos')
  })
})
